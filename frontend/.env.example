# Leave empty for same-origin requests: the dev server proxies /api to the
# backend, so calls follow whatever host the app was loaded from. This is what
# lets the app work both on the LAN and through a reverse proxy.
#
# Set this only when the backend is served from a different origin, and note
# that an HTTPS page cannot call a plain HTTP backend.
VITE_API_URL=
